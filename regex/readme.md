# Prelude
Tried doing this project in Haskell and in hindsight I think I was better off for it.

Took a bit to get used to Functional Programming but once I did it ended up being very straightforward to implement recursive methods in a recursion based language, who wouldda thought?

# Compatibility
I have verified that the binary runs on the Linux machines but I have yet to verify that the compilation works. Although I run the same set up as the Linux machines on my own computer and that the linux machines do have GHC installed, this project uses Cabal and I haven't guarenteed that the Linux machines can handle the dependencies.

**Because of that I am providing the binary with my submission**, the perl script should still be able to use it.

If you're reading this, then that means I was not able to go in and test Cabal.